services:
  pagobli:
    build: .
    ports:
      - "8080:8080"
    environment:
      PAGOBLI_ACCESS_KEY: ${PAGOBLI_ACCESS_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
